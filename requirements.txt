# Requirements pour l'application "Apprends l'Alphabet"
# Python 3.8+

# Interface graphique Qt6
PySide6>=6.5.0

# Note: Les dépendances système suivantes doivent être installées séparément:
# - libttspico-utils (Pico TTS - recommandé)
#   Installation: sudo apt-get install libttspico-utils
#
# - espeak (fallback si Pico TTS n'est pas disponible)
#   Installation: sudo apt-get install espeak